import Product from '../product';
import './productList.scss'


//{product.map(product=>{
    //return <Product product={product}></Product>
//})}

const ProductList = (props)=> {
    
    return(
        <div className="ProductList"><p>Product List</p>
        
        
        </div>
      
    );
}

export default ProductList;